/**
 * Infer catalog `source` label from an apply/job URL host.
 * Canonical catalog lives in `@nextoffer/shared/job-source`.
 */
export {
  inferJobSource,
  JOB_SOURCES,
  JOB_SOURCE_TITLES,
  JOB_SOURCE_GROUPS,
  SOURCE_MAP_VERSION,
} from '@nextoffer/shared/job-source';

/** Pipeline status values counted as applications in Analytics. */
export const REPORT_APPLIED_STATES = [
  'applied',
  'scheduled',
  'declined',
] as const;

export const REPORT_EMPTY_SOURCE = 'Other';

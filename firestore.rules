rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Product data is served only through the VPS Admin SDK. Direct Firebase
    // client access is intentionally closed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

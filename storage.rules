rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // The VPS Admin SDK owns object access. Browser SDK access remains closed;
    // short-lived signed URLs are issued by the VPS where required.
    match /{object=**} {
      allow read, write: if false;
    }
  }
}
